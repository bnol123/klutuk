name: DICE

on:
  workflow_dispatch: # Memungkinkan menjalankan workflow secara manual

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Set Git Config
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"

    - name: Run PHP Script
      env:
        keydice: ${{ secrets.KEYDICE }}
      run: php bot.php

    - name: Check for file changes
      run: |
        git status

    - name: Add changes to git
      run: |
        git add data.php arry.php  # Menambahkan kedua file
        git status                # Memeriksa status file yang sudah ditambahkan

    - name: Commit and Push Changes
      run: |
        git commit -m "Update data.php and arry.php via bot.php" || echo "No changes to commit"
        git push https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/${{ github.repository }}.git
